* Value-based window frames (`RANGE ... INTERVAL ... PRECEDING`)
* `LAG/LEAD`, `FIRST_VALUE/LAST_VALUE`
* Percentiles/quantiles (`PERCENTILE_CONT`, `APPROX_QUANTILE`)
* `ROLLUP` / `GROUPING SETS` (with `GROUPING()`)

