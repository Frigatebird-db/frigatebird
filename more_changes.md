so the thing is , if you check in @src/

the pages which get replaced by newer versions in col meta store, 

we kinda need a way to clean the old ones up and reclaim the on disk space so that the page allocator can allocate that space to someone else, this is annoying, very annoying, 
